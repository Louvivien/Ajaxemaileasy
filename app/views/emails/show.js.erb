$(document).ready(function() {

    let content = "<%= j (render 'emails/email-body') %>";
    $('.body-email').html(content);
    
    $('.mail-li<%=@email.id%>' + ' >.bold').css("font-weight", "normal");

});
